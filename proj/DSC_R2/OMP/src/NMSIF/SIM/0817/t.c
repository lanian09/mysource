#include <nmsif.h>

main ()
{
	printf ("[*] PRMT_INIT_HWALM_REQ = %d\n", PRMT_INIT_HWALM_REQ);
	printf ("[*] PRMT_INIT_SWALM_REQ = %d\n", PRMT_INIT_SWALM_REQ);

	printf ("[*] sizeof (PktHead) = %d\n", sizeof (PktHead));
	puts ("");
}
